mixin alert-success() 
    - if (messages.success)
        div(class="alert alert-success") #{messages.success}

mixin alert-error() 
    - if (messages.error)
        div(class="alert alert-danger") #{messages.error}